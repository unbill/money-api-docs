# Overview

## Introduction

> API Host

```
<%= config.host %> (production)
https://api.paybidet.com/v1/api (sandbox)
```

> Resource URL Patterns

```
/merchant
```

Welcome! The <%= config.apiName %> allows developers to integrate with Request Money, a white-label solution that provides small business's a digital way to request money from their customers.

Here you will find information on how to integrate with and use the <%= config.apiName %>. We've tried to make this documentation user-friendly and example-filled, but please drop us a line with any questions.

The <%= config.apiName %> uses standard HTTP [verbs](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) to communicate and HTTP [response codes](http://www.w3.org/Protocols/rfc2616/rfc2616-sec10.html) to indicate status and errors. All responses come in standard [JSON](http://www.json.org/). The <%= config.apiName %> is served over HTTPS to ensure data privacy, HTTP is not supported.

### Sandbox Limitations

- You cannot move real money on Sandbox.

## Gaining Access

To gain access to the <%= config.apiName %>, please contact us. After our review process is complete, you'll be provided with an API key which you can use for your requests.
